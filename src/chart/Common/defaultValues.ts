import type { FontStyle } from './types';

const fontStyle: FontStyle = {
  fontSize: 16,
  fontWeight: '500',
  fontFamily: 'Arial',
  fontColor: '#000',
};

export const defaultValues = {
  fontStyle,
};
