type Direction = 'vertical' | 'horizontal';

type Size = {
  width: number;
  height: number;
};

type Offset = {
  top: number;
  left: number;
};

export type { Direction as TabDirection, Size, Offset };
